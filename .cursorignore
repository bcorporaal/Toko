# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
# Cursor already respects .gitignore; list here only what to exclude in addition.

config.codekit3
.DS_Store
node_modules
.idea
.vscode

# Protected folders: do not edit (assets = vendor libs, dist = build output)
assets/
dist/

